% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.shiny.ports.R
\name{findFreePort}
\alias{findFreePort}
\title{Find a free port to use}
\usage{
findFreePort(start = 1024L, random = TRUE)
}
\arguments{
\item{start}{The minimum port number (default 1024 to avoid privileged ports).
When \code{random = FALSE}, sequential search starts here.}

\item{random}{Logical. If \code{TRUE} (recommended), lets the OS pick any free
port. If \code{FALSE}, searches sequentially from \code{start}.}
}
\value{
An integer: a free port number.
}
\description{
Uses the OS to find a free TCP port by binding to port 0 (which lets the
OS assign any available port), then closes the connection and returns the
assigned port. This approach works on all platforms (Windows, macOS, Linux).
}
\examples{
findFreePort(6000L, TRUE)
findFreePort(6000L)

}
