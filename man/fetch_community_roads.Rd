% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling.R
\name{fetch_community_roads}
\alias{fetch_community_roads}
\title{Fetch OSM roads for all communities}
\usage{
fetch_community_roads(
  communities_sf,
  community_id_col = "name",
  road_dir,
  road_types = c("primary", "secondary", "tertiary", "residential", "trunk",
    "unclassified"),
  timeout = 120
)
}
\arguments{
\item{communities_sf}{An \code{sf} POLYGON of community boundaries.}

\item{community_id_col}{Column name for community ID in
\code{communities_sf}. Default \code{"name"}.}

\item{road_dir}{Directory where road \code{.gpkg} files are cached.
Created if it does not exist.}

\item{road_types}{Character vector of OSM \verb{highway=*} values.}

\item{timeout}{Overpass API timeout in seconds. Default \code{120}.}
}
\value{
A named list of \code{sf} LINESTRING objects (one per
community). Communities where no roads were found have \code{NULL}.
}
\description{
Downloads and caches road linestrings for each community in a
polygon layer. Roads are saved as \code{.gpkg} files in \code{road_dir},
one per community, using the community ID as filename. On
subsequent calls, cached files are reused without re-downloading.
}
\details{
This function can be called independently before
\code{\link[=sample_communities]{sample_communities()}} to pre-download roads (e.g. while online),
then pass the same \code{road_dir} to \code{\link[=sample_communities]{sample_communities()}} for
offline use.
}
\examples{
\dontrun{
roads <- fetch_community_roads(
  communities,
  community_id_col = "name",
  road_dir = "output/roads"
)
}
}
