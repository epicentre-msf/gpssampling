% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling_manage.R
\name{create_buffers}
\alias{create_buffers}
\title{Create circular buffers around points}
\usage{
create_buffers(x, radius = 50, set = c("primary", "secondary"))
}
\arguments{
\item{x}{An \code{sf} POINT object, or a named list of communities (output
of \code{\link[=sample_communities]{sample_communities()}} or \code{\link[=split_batches]{split_batches()}}).}

\item{radius}{Buffer radius in meters. Default \code{50}.}

\item{set}{Which point set to buffer when \code{x} is a list: \code{"primary"}
(default) or \code{"secondary"}. Ignored when \code{x} is a plain \code{sf}.}
}
\value{
If \code{x} is \code{sf}: an \code{sf} POLYGON with \code{buffer_radius_m} column.
If \code{x} is a list: a named list of \code{sf} POLYGON, one per community.
}
\description{
Generates buffer polygons around sampled points using an auto-detected
UTM projection for accurate metric distances. Accepts either a single
\code{sf} POINT or a named list of communities from \code{\link[=sample_communities]{sample_communities()}}
or \code{\link[=split_batches]{split_batches()}}.
}
\examples{
\dontrun{
buffers <- create_buffers(samples, radius = 50, set = "primary")
}
}
