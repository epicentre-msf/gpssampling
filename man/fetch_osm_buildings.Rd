% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling.R
\name{fetch_osm_buildings}
\alias{fetch_osm_buildings}
\title{Fetch OSM building footprints}
\usage{
fetch_osm_buildings(area_sf, zoom = ZOOM_OSM)
}
\arguments{
\item{area_sf}{An \code{sf} polygon or multipolygon defining the area of
interest. Any CRS is accepted (transformed internally to EPSG:4326).}

\item{zoom}{Integer zoom level for tile-based queries (default \code{13L},
matching the package's \code{ZOOM_OSM} constant).}
}
\value{
An \code{sf} POLYGON with columns \code{osm_id} (character), \code{building}
(character), and \code{geometry}. Returns an empty \code{sf} with the correct
schema if no buildings are found.
}
\description{
Downloads OpenStreetMap building footprints for a given area using
tile-based Overpass API queries.
}
\examples{
\dontrun{
state <- sf::st_read("boundary.gpkg")
buildings <- fetch_osm_buildings(state)
}
}
