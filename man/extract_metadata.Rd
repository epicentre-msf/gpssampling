% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling_manage.R
\name{extract_metadata}
\alias{extract_metadata}
\title{Extract metadata from batched sampling results}
\usage{
extract_metadata(primary, secondary = NULL)
}
\arguments{
\item{primary}{Output of \code{\link[=split_batches]{split_batches()}} for the primary set.}

\item{secondary}{Output of \code{\link[=split_batches]{split_batches()}} for the secondary
set. Default \code{NULL} (primary only).}
}
\value{
A \code{data.frame} with columns: \code{community}, \code{point_id},
\code{named_point_id} (when available), \code{assigned_batch}, \code{set}.
Carries two attributes: \code{buffer_size} (named numeric vector of
per-community buffer radii) and \code{n_teams} (named integer vector
of per-community batch counts).
}
\description{
Combines primary and (optionally) secondary batched results into a
single data frame with per-point metadata. Useful for creating
field assignment sheets.
}
\examples{
\dontrun{
meta <- extract_metadata(primary_batches, secondary_batches)
attr(meta, "buffer_size")
attr(meta, "n_teams")
}
}
