% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling.R
\name{sample_communities}
\alias{sample_communities}
\title{Sample buildings across communities}
\usage{
sample_communities(
  buildings_list,
  n_required,
  min_distance = 50,
  seed,
  joint = FALSE,
  print_table = TRUE,
  road_types = c("primary", "secondary", "tertiary", "residential", "trunk",
    "unclassified"),
  road_dir = NULL
)
}
\arguments{
\item{buildings_list}{Named list of \code{sf} POINT objects (output of
\code{\link[=crop_buildings]{crop_buildings()}}).}

\item{n_required}{Named integer vector of required sample sizes per
community. Names must match \code{buildings_list}. A single unnamed
integer applies the same size to all communities.}

\item{min_distance}{Numeric, minimum distance in meters between any
two selected points. Default \code{50}.}

\item{seed}{Integer RNG seed for reproducibility (\strong{required}, no
default). A per-community seed is derived from \code{seed} and the
community name, so adding or removing a community does not change
the selection in other communities. Results are reproducible across
machines given the same seed, input data, and R version (>= 3.6.0).
R 3.6.0 changed the default sampling algorithm
(\code{sample.kind = "Rejection"}), so results from R < 3.6 and
R >= 3.6 will differ even with the same seed.}

\item{joint}{Logical. If \code{TRUE}, primary and secondary points are
drawn together in a single pass, enforcing the minimum distance
across both sets. This reduces clustering in the secondary points.
The first \code{n_required} drawn become primary; the rest become
secondary. Default \code{FALSE} (independent draws).}

\item{print_table}{Logical. If \code{TRUE} (default), prints a
\code{\link[flextable:flextable]{flextable::flextable()}} summary at the end of sampling with
per-community statistics: buildings available, points drawn,
distance metrics, constraint violations, and coverage.}

\item{road_types}{Character vector of OSM \verb{highway=*} values used
for the post-selection proximity ordering.}

\item{road_dir}{Optional directory for cached road files. If
provided, roads are read from / saved to
\verb{road_dir/\{community_name\}.gpkg}. Use \code{\link[=fetch_community_roads]{fetch_community_roads()}}
to pre-download roads. Default \code{NULL} (no caching).}
}
\value{
A named list. Each community element contains:
\verb{$buildings} (all candidates), \verb{$primary} (selected points with
\code{selection_order} and \code{point_id}), \verb{$secondary} (replacement
points with \code{selection_order} and \code{point_id}, at most \code{n_required}
per community), \verb{$min_distance}, and \verb{$seed}. Both primary and
secondary are ordered by road proximity (nearest-neighbour chain).
The \code{point_id} column is globally unique across all communities
and sets: primary IDs are numbered 1..N_total_primary, secondary
IDs continue from N_total_primary + 1. When \code{print_table = TRUE},
the result carries two attributes: \code{attr(, "summary_table")} (a
\code{\link[flextable:flextable]{flextable::flextable()}} object ready for rendering in reports)
and \code{attr(, "summary_df")} (the underlying data frame). Access
via \code{attr(result, "summary_table")}.
}
\description{
Top-level function that orchestrates reproducible spatial sampling
across all communities. Points are selected randomly with minimum-
distance constraints, then reordered by proximity to roads for
efficient field work.
}
\examples{
\dontrun{
samples <- sample_communities(
  buildings_list,
  n_required = c(community_one = 30, community_two = 80),
  min_distance = 50,
  seed = 12345L
)

# Joint sampling (less clustered secondary points)
samples <- sample_communities(
  buildings_list,
  n_required = c(community_one = 30, community_two = 80),
  min_distance = 50,
  seed = 12345L,
  joint = TRUE
)
}
}
