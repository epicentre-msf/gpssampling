% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling.R
\name{sample_communities}
\alias{sample_communities}
\title{Sample buildings across communities}
\usage{
sample_communities(
  buildings_list,
  n_required,
  min_distance = 50,
  default_distance = 50,
  seed,
  joint = FALSE,
  point_id_digits = NULL,
  print_table = TRUE,
  road_types = c("primary", "secondary", "tertiary", "residential", "trunk",
    "unclassified"),
  road_dir = NULL,
  starting_point = 1L,
  buffer_overlap = TRUE
)
}
\arguments{
\item{buildings_list}{Named list of \code{sf} POINT objects (output of
\code{\link[=crop_buildings]{crop_buildings()}}).}

\item{n_required}{Named integer vector of required sample sizes per
community. Names must match \code{buildings_list}. A single unnamed
integer applies the same size to all communities.}

\item{min_distance}{Minimum distance in meters between any two
selected points. Either a single numeric value (applied to all
communities) or a \strong{named numeric vector} with per-community
distances. Names must match entries in \code{buildings_list}. Any
community not found in the named vector uses
\code{default_distance}. Default \code{50}.}

\item{default_distance}{Numeric fallback distance in meters for
communities not listed in a named \code{min_distance} vector.
Ignored when \code{min_distance} is a scalar. Default \code{50}.}

\item{seed}{Integer RNG seed for reproducibility (\strong{required}, no
default). A per-community seed is derived from \code{seed} and the
community name, so adding or removing a community does not change
the selection in other communities. Results are reproducible across
machines given the same seed, input data, and R version (>= 3.6.0).
R 3.6.0 changed the default sampling algorithm
(\code{sample.kind = "Rejection"}), so results from R < 3.6 and
R >= 3.6 will differ even with the same seed.}

\item{joint}{Logical. If \code{TRUE}, primary and secondary points are
drawn together in a single pass, enforcing the minimum distance
across both sets. This reduces clustering in the secondary points.
The first \code{n_required} drawn become primary; the rest become
secondary. Default \code{FALSE} (independent draws).}

\item{point_id_digits}{Integer or \code{NULL}. When set, creates a
\code{named_point_id} column with zero-padded IDs of the given width
(e.g., \code{point_id_digits = 3} produces \code{"001"}, \code{"002"}, ...).
Used as display name in GPX/KML exports. Default \code{NULL} (no
padding, exports use numeric \code{point_id}).}

\item{print_table}{Logical. If \code{TRUE} (default), prints a
\code{\link[flextable:flextable]{flextable::flextable()}} summary at the end of sampling with
per-community statistics: buildings available, points drawn,
distance metrics, constraint violations, and coverage.}

\item{road_types}{Character vector of OSM \verb{highway=*} values used
for the post-selection proximity ordering.}

\item{road_dir}{Optional directory for cached road files. If
provided, roads are read from / saved to
\verb{road_dir/\{community_name\}.gpkg}. Use \code{\link[=fetch_community_roads]{fetch_community_roads()}}
to pre-download roads. Default \code{NULL} (no caching).}

\item{starting_point}{Integer. Starting number for \code{point_id} assignment.
Useful when combining results from multiple survey rounds where IDs must
not restart at 1. Default \code{1L}.}

\item{buffer_overlap}{Logical. Controls whether the circular buffers of
radius \code{min_distance} around any two selected points are allowed to
overlap. If \code{TRUE} (default), the standard \code{min_distance} exclusion
radius is used.
If \code{FALSE}, the exclusion radius is doubled to \code{2 * min_distance},
preventing any two buffers from touching or overlapping. Set to \code{FALSE}
when field workers are instructed to sample any valid building inside the
buffer if the original point is invalid, and you need to guarantee that
no replacement building could fall within another point's buffer.}
}
\value{
A named list. Each community element contains:
\verb{$buildings} (all candidates), \verb{$primary} (selected points with
\code{selection_order} and \code{point_id}), \verb{$secondary} (replacement
points with \code{selection_order} and \code{point_id}, at most \code{n_required}
per community), \verb{$min_distance} (the resolved per-community
distance in meters), and \verb{$seed}. When \code{point_id_digits} is set,
primary and secondary also carry a \code{named_point_id} column with
zero-padded string IDs. Both primary and
secondary are ordered by road proximity (nearest-neighbour chain).
The \code{point_id} column is globally unique across all communities
and sets: primary IDs are numbered 1..N_total_primary, secondary
IDs continue from N_total_primary + 1. When \code{print_table = TRUE},
the result carries two attributes: \code{attr(, "summary_table")} (a
\code{\link[flextable:flextable]{flextable::flextable()}} object ready for rendering in reports)
and \code{attr(, "summary_df")} (the underlying data frame). Access
via \code{attr(result, "summary_table")}.
}
\description{
Top-level function that orchestrates reproducible spatial sampling
across all communities. Points are selected randomly with minimum-
distance constraints, then reordered by proximity to roads for
efficient field work.
}
\examples{
\dontrun{
# Uniform distance for all communities
samples <- sample_communities(
  buildings_list,
  n_required = c(community_one = 30, community_two = 80),
  min_distance = 50,
  seed = 12345L
)

# Per-community distances (dense vs. sparse areas)
samples <- sample_communities(
  buildings_list,
  n_required = c(community_one = 30, community_two = 80),
  min_distance = c(community_one = 30, community_two = 80),
  seed = 12345L
)

# Per-community with a default fallback for unlisted communities
samples <- sample_communities(
  buildings_list,
  n_required = c(community_one = 30, community_two = 80),
  min_distance = c(community_one = 30),
  default_distance = 60,
  seed = 12345L
)

# Retrieve per-community distance from result
samples$community_one$min_distance # 30
samples$community_two$min_distance # 60 (default_distance)

# Joint sampling (less clustered secondary points)
samples <- sample_communities(
  buildings_list,
  n_required = c(community_one = 30, community_two = 80),
  min_distance = 50,
  seed = 12345L,
  joint = TRUE
)

# Shift point IDs to start at 300 (e.g., continuing from a prior round)
samples <- sample_communities(
  buildings_list,
  n_required = c(community_one = 30, community_two = 80),
  min_distance = 50,
  seed = 12345L,
  starting_point = 300L
)
samples$community_one$primary$point_id # starts at 300

# No buffer overlap: any two buffers of radius 50m never touch
# (effective exclusion radius becomes 100m = 2 * 50)
samples <- sample_communities(
  buildings_list,
  n_required = c(community_one = 30, community_two = 80),
  min_distance = 50,
  seed = 12345L,
  buffer_overlap = FALSE
)
}
}
