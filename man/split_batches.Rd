% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling_manage.R
\name{split_batches}
\alias{split_batches}
\title{Split sample points into batches}
\usage{
split_batches(samples_list, n_batches, set = c("primary", "secondary"))
}
\arguments{
\item{samples_list}{Named list of communities, output of
\code{\link[=sample_communities]{sample_communities()}}.}

\item{n_batches}{Integer (applied to all communities) or named integer
vector (per community). E.g., \code{5L} or \code{c(kamboma = 5, largo = 3)}.}

\item{set}{Which point set to split: \code{"primary"} (default) or
\code{"secondary"}.}
}
\value{
A named list of \code{sf} POINT objects, each with an added
\code{assigned_batch} column (integer, 1 to n_batches).
}
\description{
Distributes primary or secondary sample points into numbered batches
using round-robin assignment. Useful for dividing field work across
multiple teams.
}
\examples{
\dontrun{
batched <- split_batches(samples, n_batches = 5L, set = "primary")
}
}
