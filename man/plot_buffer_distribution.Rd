% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling_map.R
\name{plot_buffer_distribution}
\alias{plot_buffer_distribution}
\title{Plot the distribution of buildings per buffer}
\usage{
plot_buffer_distribution(
  export_result,
  fill_color = "#5B9BD5",
  mean_color = "#D94F4F",
  median_color = "#2E8B57",
  binwidth = NULL,
  title = "Buildings per Buffer",
  subtitle = NULL,
  free_y = TRUE
)
}
\arguments{
\item{export_result}{The manifest returned by \code{\link[=export_points]{export_points()}} when
called with \code{print_table = TRUE}. Must carry a \code{buffer_details}
attribute (data frame with columns \code{community}, \code{buffer_idx},
\code{n_buildings}, \code{buffer_radius_m}).}

\item{fill_color}{Bar fill color. Default \code{"#5B9BD5"}.}

\item{mean_color}{Color for the mean line. Default \code{"#D94F4F"}.}

\item{median_color}{Color for the median line. Default \code{"#2E8B57"}.}

\item{binwidth}{Histogram bin width. If \code{NULL} (default), ggplot2
picks an automatic value.}

\item{title}{Plot title. Default \code{"Buildings per Buffer"}.}

\item{subtitle}{Plot subtitle. If \code{NULL} (default), auto-generated
from community count and total buffer count.}

\item{free_y}{Logical. If \code{TRUE} (default), facet y-axes are free
(communities with different buffer counts get their own scale).}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a ggplot histogram showing how many buildings fall within
each buffer zone, faceted by community. Useful for assessing
sampling density uniformity across communities. Vertical dashed
lines indicate the median and mean per community.
}
\details{
Requires \code{ggplot2} (in Suggests).
}
\examples{
\dontrun{
manifest <- export_points(batched, "output", print_table = TRUE)
plot_buffer_distribution(manifest)
}
}
