% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling.R
\name{crop_buildings}
\alias{crop_buildings}
\title{Crop buildings to community polygons}
\usage{
crop_buildings(buildings_sf, communities_sf, community_id_col = "name")
}
\arguments{
\item{buildings_sf}{An \code{sf} POLYGON of building footprints.}

\item{communities_sf}{An \code{sf} POLYGON/MULTIPOLYGON with one row per
community.}

\item{community_id_col}{Character. Name of the column in
\code{communities_sf} that uniquely identifies each community.}
}
\value{
A named list of \code{sf} POINT objects (keyed by community name).
Each has columns: \code{id} (integer), \code{community} (character), and
\code{geometry} (sfc_POINT). If \code{buildings_sf} has an \code{osm_id} column,
it is preserved.
}
\description{
Intersects building footprints with community polygons, converts to
centroids, and returns a named list (one \code{sf} POINT per community).
Points are sorted deterministically by longitude then latitude.
}
\examples{
\dontrun{
buildings_list <- buildings |>
  crop_buildings(communities, community_id_col = "name")
}
}
