pkg_infos <- list(
  title = "GPS Sampling",
  name = "gpssampling",
  url_issue = "https://github.com/epicentre-msf/gpssampling/issues"
)

pkg_url <- "gpssampling"

knitr::opts_chunk$set(
  collapse = TRUE,
  comment = '#>',
  out.width = '100%'
)

options(
  tibble.print_min = 5L,
  tibble.print_max = 5L,
  width = 150L
)
