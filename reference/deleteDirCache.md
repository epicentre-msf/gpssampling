# Delete the directory cache

This internal function deletes the directory cache using the
[`fs::dir_delete`](https://fs.r-lib.org/reference/delete.html) function
and the `getDirApp` function to get the directory path.

## Usage

``` r
deleteDirCache()
```
