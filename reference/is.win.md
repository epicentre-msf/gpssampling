# Check if the current operating system is Windows

This function checks if the current operating system is Windows.

## Usage

``` r
is.win()
```

## Value

`TRUE` if the current operating system is Windows, `FALSE` otherwise.

## Examples

``` r
is.win()
#> Error in is.win(): could not find function "is.win"
```
