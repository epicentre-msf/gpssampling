# Check if the R platform is x64

This function checks if the R platform is x64.

## Usage

``` r
is.x64()
```

## Value

`TRUE` if the R platform is x64, `FALSE` otherwise.

## Examples

``` r
is.x64() # Returns TRUE if R platform is x64
#> Error in is.x64(): could not find function "is.x64"
```
