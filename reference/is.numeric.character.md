# Check if values in a vector are numeric or character

This function determines whether the values in a vector are numeric or
character. It can handle atomic vectors or lists.

## Usage

``` r
# S3 method for class 'character'
is.numeric(x)
```

## Arguments

- x:

  A vector or list

## Value

A logical vector indicating whether each element in `x` is numeric or
character
