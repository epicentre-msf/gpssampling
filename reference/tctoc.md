# Tictoc function

This function evaluates the given expression and measures the execution
time.

## Usage

``` r
tctoc(expr)
```

## Arguments

- expr:

  An expression to be evaluated.

## Value

The result of evaluating the expression.

## Details

This function internally uses the tic() and toc() functions from the
tictoc package to measure the execution time of the given expression.
