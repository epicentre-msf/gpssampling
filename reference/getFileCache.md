# Get File Cache

This function returns the path of the specified file cache.

## Usage

``` r
getFileCache(name, ext = "feather")
```

## Arguments

- name:

  The name of the file cache.

- ext:

  The extension of the file cache. Default value is 'feather'.

## Value

The path of the specified file cache.
