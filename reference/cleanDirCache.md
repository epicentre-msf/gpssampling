# Clean directory cache

This function deletes the cache folder in the current working directory.

## Usage

``` r
cleanDirCache()
```

## Examples

``` r
cleanDirCache()
#> Error in cleanDirCache(): could not find function "cleanDirCache"
```
